name: Validar sintaxe HTML
on: [push]
jobs:
  validar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Usar tidy para validar
        run: sudo apt-get install -y tidy && tidy -errors index.html
